package test

import (
	"fmt"
	"go-mysql-transfer/domain/po"
	"testing"
)

func TestIndex(t *testing.T) {
	//rules := make([]string, 2)
	//rules[0] = "a"
	//rules[1] = "b"
	//
	//fmt.Println(rules[3])

	var entity po.EndpointInfo
	fmt.Println(entity)

}
